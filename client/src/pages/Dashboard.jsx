/**
 * Dashboard page — implemented in Phase 4 (Remaining Features).
 *
 * Displays:
 *   Resident: total complaints, counts by status, overdue count
 *   Admin:    same as resident + counts by category across all complaints
 */
export default function Dashboard() {
  return (
    <div className="page">
      <div className="page-header">
        <h1>Dashboard</h1>
      </div>
      <p className="text-muted">Statistics and overview — Phase 4</p>
    </div>
  );
}
