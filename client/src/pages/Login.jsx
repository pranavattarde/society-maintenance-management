/**
 * Login page — implemented in Phase 2 (Authentication).
 */
export default function Login() {
  return (
    <div className="auth-page">
      <div className="auth-card card">
        <h1 className="auth-title">Sign In</h1>
        <p className="text-muted text-sm">Authentication form — Phase 2</p>
      </div>
    </div>
  );
}
